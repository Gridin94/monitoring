apiVersion: 1

providers:
  - name: 'node'
    folder: 'node'
    type: file
    options:
      path:  /etc/grafana/provisioning/dashbords/node.json
  # - name: 'prometheus'
  #   folder: 'prometheus'
  #   type: file
  #   options:
  #     path:  /etc/grafana/provisioning/dashbords/prometheus.json